{
  "extends": "astro/tsconfigs/strict",
  "include": [
    ".astro/types.d.ts",
    "**/*"
  ],
  "//": "functions/ corre en el runtime de Cloudflare Workers, no en Astro: tiene su propio tsconfig.",
  "exclude": [
    "dist",
    "functions"
  ]
}